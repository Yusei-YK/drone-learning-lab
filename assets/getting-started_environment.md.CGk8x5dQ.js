import{_ as a,o as e,c as t,a2 as i}from"./chunks/framework.9ngYh_O2.js";const k=JSON.parse('{"title":"第一步：搭一个不会污染宿主机的环境","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/environment.md","filePath":"getting-started/environment.md","lastUpdated":1788501998000}'),n={name:"getting-started/environment.md"};function d(o,s,l,p,c,h){return e(),t("div",null,[...s[0]||(s[0]=[i(`<h1 id="第一步-搭一个不会污染宿主机的环境" tabindex="-1">第一步：搭一个不会污染宿主机的环境 <a class="header-anchor" href="#第一步-搭一个不会污染宿主机的环境" aria-label="Permalink to &quot;第一步：搭一个不会污染宿主机的环境&quot;">​</a></h1><div class="tip custom-block"><p class="custom-block-title">这一页你会得到什么</p><p>一个装好 ROS 2 Humble 的 Docker 镜像，以及&quot;为什么非得这么做&quot;的完整理由。做完之后你的宿主机系统一个包都没多装。 预计时间：镜像构建 15~30 分钟（取决于网速），其中你只需要敲 1 条命令。</p></div><h2 id="_1-为什么必须用-docker" tabindex="-1">1. 为什么必须用 Docker <a class="header-anchor" href="#_1-为什么必须用-docker" aria-label="Permalink to &quot;1. 为什么必须用 Docker&quot;">​</a></h2><p>这不是为了炫技，是被逼的。</p><p><strong>ROS 2 的版本和 Ubuntu 的版本是硬绑定的</strong>：</p><table tabindex="0"><thead><tr><th>ROS 2 发行版</th><th>官方绑定的 Ubuntu</th></tr></thead><tbody><tr><td>Humble Hawksbill</td><td>22.04 (Jammy)</td></tr><tr><td>Jazzy Jalisco</td><td>24.04 (Noble)</td></tr></tbody></table><p>本机是 <strong>Ubuntu 24.04.3 + ROS 2 Jazzy</strong>，而 EGO-Planner 的 <code>ros2_version</code> 分支是针对 <strong>Humble</strong> 写的。你没法在 24.04 上直接 <code>apt install ros-humble-*</code>——官方源里根本没有为 24.04 编译的 Humble 包。</p><p>三条路，为什么选第三条：</p><table tabindex="0"><thead><tr><th>方案</th><th>问题</th></tr></thead><tbody><tr><td>重装成 22.04 / 装双系统</td><td>代价太大，而且以后学 PX4、Isaac 还要换回新系统</td></tr><tr><td>强行在 24.04 上编译 Humble</td><td>依赖地狱，几乎不可能干净收场</td></tr><tr><td><strong>Docker 跑一个 22.04 + Humble 容器</strong> ✅</td><td>宿主机零污染，删掉容器就等于什么都没发生</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">记忆方法</p><p>把 Docker 想成&quot;一间租来的房子&quot;：里面怎么装修（装什么包、改什么配置）都不影响你自己的房子（宿主机）。退租（<code>docker rm</code>）之后一点痕迹都不留。</p></div><h2 id="_2-宿主机基线-本机实测值" tabindex="-1">2. 宿主机基线（本机实测值） <a class="header-anchor" href="#_2-宿主机基线-本机实测值" aria-label="Permalink to &quot;2. 宿主机基线（本机实测值）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>项目</th><th>值</th><th>怎么查</th></tr></thead><tbody><tr><td>系统</td><td>Ubuntu 24.04.3 LTS</td><td><code>cat /etc/os-release</code></td></tr><tr><td>宿主机 ROS 2</td><td>Jazzy（<strong>不参与本项目</strong>）</td><td><code>ls /opt/ros/</code></td></tr><tr><td>Docker</td><td>29.1.3</td><td><code>sudo docker version</code></td></tr><tr><td>CPU</td><td>13th Gen Intel Core i9-13980HX</td><td><code>grep &#39;model name&#39; /proc/cpuinfo</code></td></tr><tr><td>GPU</td><td>NVIDIA RTX 4060 Laptop 8 GB</td><td><code>nvidia-smi</code></td></tr><tr><td>内存</td><td>16 GB</td><td><code>free -h</code></td></tr><tr><td>桌面会话</td><td>X11，<code>DISPLAY=:1</code></td><td><code>echo $XDG_SESSION_TYPE</code></td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">桌面会话是 X11 还是 Wayland 很重要</p><p>后面要把容器里的 RViz 画到你的屏幕上，X11 才好办。如果 <code>echo $XDG_SESSION_TYPE</code> 输出 <code>wayland</code>，图形转发的做法不一样。本项目全程是 X11。</p></div><h2 id="_3-让自己能敲-docker-命令" tabindex="-1">3. 让自己能敲 docker 命令 <a class="header-anchor" href="#_3-让自己能敲-docker-命令" aria-label="Permalink to &quot;3. 让自己能敲 docker 命令&quot;">​</a></h2><p>刚装好 Docker 时，普通用户执行 <code>docker ps</code> 会报：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>permission denied while trying to connect to the Docker daemon socket</span></span></code></pre></div><p>网上最常见的建议是 <code>sudo usermod -aG docker $USER</code>。<strong>本项目故意没这么做。</strong></p><p>原因：<code>docker</code> 组的权限<strong>等价于 root</strong>。加入这个组之后，任何程序都能不弹密码地执行 <code>docker run -v /:/host ...</code> 把你的整个硬盘挂进容器。这是一次性、永久、无提示的提权。</p><p>本项目采用的替代方案是一条<strong>范围收窄</strong>的免密规则（这条命令需要你自己执行，会要求输入密码）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;yusei ALL=(root) NOPASSWD: /usr/bin/docker&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tee</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /etc/sudoers.d/90-yusei-docker</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> chmod</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 440</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /etc/sudoers.d/90-yusei-docker</span></span></code></pre></div><p>把 <code>yusei</code> 换成你自己的用户名（<code>whoami</code> 可以查）。</p><p>验证：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -l</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -3</span></span></code></pre></div><p>期望看到：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>(root) NOPASSWD: /usr/bin/docker</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">必须诚实说清楚的两件事</p><ol><li><strong>这条规则只覆盖 <code>/usr/bin/docker</code></strong>。所以 <code>sudo apt install ...</code> 之类的命令<strong>仍然会要求密码</strong>，这是故意的——范围越小越安全。</li><li><strong>免密 docker 本质上仍然等于 root 权限</strong>（能挂载任意目录）。它比加入 <code>docker</code> 组好，只是因为范围明确、可审计、随时能 <code>sudo rm /etc/sudoers.d/90-yusei-docker</code> 撤销。<strong>这是&quot;个人单机学习环境&quot;的折中，不要在共享机器或生产服务器上这么配。</strong></li></ol></div><p>从此本项目所有 docker 命令都写成 <code>sudo docker ...</code>。看到 <code>sudo</code> 不要慌，它就是这么来的。</p><h2 id="_4-镜像里装了什么-逐行读-dockerfile" tabindex="-1">4. 镜像里装了什么（逐行读 Dockerfile） <a class="header-anchor" href="#_4-镜像里装了什么-逐行读-dockerfile" aria-label="Permalink to &quot;4. 镜像里装了什么（逐行读 Dockerfile）&quot;">​</a></h2><div class="language-docker vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">docker</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ros:humble-ros-base-jammy</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ENV</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DEBIAN_FRONTEND=noninteractive \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RMW_IMPLEMENTATION=rmw_cyclonedds_cpp</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">RUN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> apt-get update &amp;&amp; apt-get install -y --no-install-recommends \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    build-essential cmake git \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    python3-colcon-common-extensions python3-rosdep python3-vcstool \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ros-humble-desktop ros-humble-rmw-cyclonedds-cpp \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    libpcl-dev libvtk9-qt-dev libarmadillo-dev \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &amp;&amp; rm -rf /var/lib/apt/lists/*</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">RUN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> apt-get update &amp;&amp; apt-get install -y --no-install-recommends \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ros-humble-pcl-ros \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &amp;&amp; rm -rf /var/lib/apt/lists/*</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">RUN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rosdep init 2&gt;/dev/null || true</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WORKDIR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /workspace</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">COPY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> docker/entrypoint.sh /entrypoint.sh</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ENTRYPOINT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/entrypoint.sh&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CMD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>每一段为什么存在：</p><table tabindex="0"><thead><tr><th>内容</th><th>作用</th><th>不装会怎样</th></tr></thead><tbody><tr><td><code>FROM ros:humble-ros-base-jammy</code></td><td>官方 Humble 基础镜像，自带 Ubuntu 22.04</td><td>—</td></tr><tr><td><code>RMW_IMPLEMENTATION=rmw_cyclonedds_cpp</code></td><td>指定 DDS 中间件，<strong>写在镜像里，容器和宿主机都一致</strong></td><td>两边中间件不同时互相看不见话题</td></tr><tr><td><code>build-essential cmake git</code></td><td>编译器和构建工具</td><td>无法编译 C++</td></tr><tr><td><code>python3-colcon-common-extensions</code></td><td>ROS 2 的构建工具 <code>colcon</code></td><td>无法编译工作空间</td></tr><tr><td><code>python3-rosdep</code></td><td>自动解析并安装依赖</td><td>只能手工一个个找依赖</td></tr><tr><td><code>ros-humble-desktop</code></td><td>包含 RViz2 等图形工具</td><td>没有 RViz，看不到画面</td></tr><tr><td><code>ros-humble-rmw-cyclonedds-cpp</code></td><td>CycloneDDS 的实现本体</td><td>上面那个环境变量会指向一个不存在的中间件</td></tr><tr><td><code>libpcl-dev</code></td><td>点云库，EGO 的地图和点云处理全靠它</td><td>编译报找不到 PCL</td></tr><tr><td><code>libvtk9-qt-dev</code></td><td>PCL 可视化依赖的 VTK</td><td>PCL 相关目标链接失败</td></tr><tr><td><code>libarmadillo-dev</code></td><td>线性代数库，<code>pose_utils</code> 等包需要</td><td>编译报找不到 Armadillo</td></tr><tr><td><code>ros-humble-pcl-ros</code></td><td>PCL 与 ROS 消息的桥接</td><td>编译报找不到 <code>pcl_ros</code>（这是踩过的坑）</td></tr><tr><td><code>rm -rf /var/lib/apt/lists/*</code></td><td>删掉 apt 缓存</td><td>镜像白白大几百 MB</td></tr><tr><td><code>rosdep init || true</code></td><td>初始化 rosdep 数据库，失败也不中断构建</td><td>重复构建时会因&quot;已初始化&quot;而报错中断</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">记忆方法：为什么分成两个 RUN</p><p>Docker 的每一条 <code>RUN</code> 是一层缓存。<code>ros-humble-pcl-ros</code> 是后来才发现缺的（编译报错才知道），单独放一层意味着<strong>重新构建时前面那一大层不用重跑</strong>，省十几分钟。这是 Dockerfile 的通用技巧：<strong>新增的、可能还要改的东西放最后面。</strong></p></div><div class="warning custom-block"><p class="custom-block-title">关于 <code>ros-humble-desktop</code> 的取舍</p><p>它体积很大（镜像最终 4.84 GB），装的东西远超&quot;跑仿真&quot;所需。但它包含 RViz2，而这个项目<strong>必须看图</strong>。用 <code>ros-humble-ros-base</code> + 单独装 <code>ros-humble-rviz2</code> 理论上更小，本项目没有走这条路——<strong>已经能跑通的环境不要为了省几百 MB 去折腾</strong>。</p></div><h2 id="_5-entrypoint-sh-为什么需要它" tabindex="-1">5. entrypoint.sh：为什么需要它 <a class="header-anchor" href="#_5-entrypoint-sh-为什么需要它" aria-label="Permalink to &quot;5. entrypoint.sh：为什么需要它&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#!/usr/bin/env bash</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">set</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">source</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/ros/humble/setup.bash</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [[ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /workspace/install/setup.bash ]]; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">then</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  source</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /workspace/install/setup.bash</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">fi</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$@</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span></code></pre></div><p>ROS 2 的命令（<code>ros2</code>、<code>colcon</code>）不是全局可用的，必须先 <code>source</code> 环境脚本，往 <code>PATH</code>、<code>AMENT_PREFIX_PATH</code> 等变量里塞路径。每次进容器手动 source 两次太烦，所以塞进 ENTRYPOINT——<strong>容器一启动就自动 source 好。</strong></p><p>三个细节：</p><ul><li><code>if [[ -f ... ]]</code>：第一次构建时 <code>install/</code> 还不存在（还没编译），不加判断会直接报错退出，连 shell 都进不去。</li><li><code>exec &quot;$@&quot;</code>：把 <code>docker run</code> 后面跟的命令接过来执行。<code>exec</code> 会<strong>替换</strong>当前进程而不是新开一个子进程，这样容器里的 PID 1 就是你的程序，<code>docker stop</code> 发的信号能直接送到它手上。</li><li><code>set -e</code>：source 失败就立刻退出，而不是带着半个环境继续跑。</li></ul><div class="danger custom-block"><p class="custom-block-title">这一节是最容易踩的坑，务必记住</p><p>ENTRYPOINT <strong>只在 <code>docker run</code> 时执行，<code>docker exec</code> 不执行</strong>。所以：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ego_sim</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # ❌ ros2: command not found</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ego_sim</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -lc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &#39;source /opt/ros/humble/setup.bash &amp;&amp; \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   source /workspace/install/setup.bash &amp;&amp; ros2 node list&#39;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # ✅</span></span></code></pre></div><p>看到 <code>ros2: command not found</code> <strong>不要怀疑 ROS 装坏了</strong>，先看是不是用了 <code>docker exec</code> 而忘了 source。</p></div><h2 id="_6-构建镜像-你只需要敲这一条" tabindex="-1">6. 构建镜像（你只需要敲这一条） <a class="header-anchor" href="#_6-构建镜像-你只需要敲这一条" aria-label="Permalink to &quot;6. 构建镜像（你只需要敲这一条）&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#!/usr/bin/env bash</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">set</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -euo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pipefail</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dirname</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;)&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> local/ego-planner-humble:latest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span></code></pre></div><p>存成 <code>build-image.sh</code>，然后：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chmod</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> +x</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build-image.sh</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./build-image.sh</span></span></code></pre></div><p>脚本里三个写法值得学：</p><table tabindex="0"><thead><tr><th>写法</th><th>含义</th><th>为什么要</th></tr></thead><tbody><tr><td><code>set -euo pipefail</code></td><td>出错即停 / 用未定义变量即停 / 管道中任一环失败即停</td><td>不加的话前面的步骤失败了后面还会继续跑，最后报一个完全无关的错</td></tr><tr><td><code>cd &quot;$(dirname &quot;$0&quot;)&quot;</code></td><td>切换到脚本自己所在的目录</td><td>这样你在任何目录下执行它都对，<code>docker build .</code> 的 <code>.</code> 才指向 Dockerfile 所在处</td></tr><tr><td><code>-t local/ego-planner-humble:latest</code></td><td>给镜像起名字</td><td>不起名就只有一串 hash，后面 <code>docker run</code> 没法写</td></tr></tbody></table><p><code>local/</code> 前缀是自己加的约定，表示&quot;这是本地构建的，不是从 Docker Hub 拉的&quot;。</p><div class="warning custom-block"><p class="custom-block-title">构建要花 15~30 分钟，而且很吃网络</p><p>中间会下载几 GB 的 apt 包。<strong>中途失败绝大多数是网络问题，不是配置问题</strong>——本项目遇到的第一个坑就是代理，详见 <a href="/drone-learning-lab/debugging/docker-build">构建期问题</a>。失败了直接重跑 <code>./build-image.sh</code>，Docker 会复用已完成的层，不用从头开始。</p></div><h2 id="_7-验收-证明镜像是对的" tabindex="-1">7. 验收：证明镜像是对的 <a class="header-anchor" href="#_7-验收-证明镜像是对的" aria-label="Permalink to &quot;7. 验收：证明镜像是对的&quot;">​</a></h2><p>构建完成不等于对。跑这两条命令验收。</p><p><strong>第一条：镜像存在，体积正常</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> images</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --format</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;table {{.Repository}}:{{.Tag}}\\t{{.Size}}&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> local/ego-planner-humble:latest</span></span></code></pre></div><p>本机真实输出【运行验证】：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>REPOSITORY:TAG                    SIZE</span></span>
<span class="line"><span>local/ego-planner-humble:latest   4.84GB</span></span></code></pre></div><p><strong>第二条：进去问它自己是谁</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> local/ego-planner-humble:latest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -lc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &#39;echo ROS_DISTRO=$ROS_DISTRO; echo RMW=$RMW_IMPLEMENTATION; \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   which ros2 colcon rviz2; lsb_release -ds&#39;</span></span></code></pre></div><p>本机真实输出【运行验证】：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ROS_DISTRO=humble</span></span>
<span class="line"><span>RMW=rmw_cyclonedds_cpp</span></span>
<span class="line"><span>/opt/ros/humble/bin/ros2</span></span>
<span class="line"><span>/usr/bin/colcon</span></span>
<span class="line"><span>/opt/ros/humble/bin/rviz2</span></span>
<span class="line"><span>Ubuntu 22.04.5 LTS</span></span></code></pre></div><p>逐行确认了 5 件事：Humble 装上了、CycloneDDS 生效了、<code>ros2</code> 有、<code>colcon</code> 有、RViz2 有。</p><div class="tip custom-block"><p class="custom-block-title">最后一行最值得看</p><p>容器里是 <strong>Ubuntu 22.04.5</strong>，而你的宿主机是 <strong>24.04.3</strong>。同一台电脑、同一个内核、两个不同版本的 Ubuntu 用户空间同时运行——这就是本页开头那个&quot;版本硬绑定&quot;难题的解法，也是整个 Docker 方案的价值所在，一行输出就证明了。</p></div><p><code>--rm</code> 表示&quot;跑完就删掉这个容器&quot;。验收这种一次性命令都应该加 <code>--rm</code>，否则会攒下一堆 <code>Exited</code> 的僵尸容器（<code>sudo docker ps -a</code> 能看到）。</p><h2 id="_8-三个脚本把环境固化下来" tabindex="-1">8. 三个脚本把环境固化下来 <a class="header-anchor" href="#_8-三个脚本把环境固化下来" aria-label="Permalink to &quot;8. 三个脚本把环境固化下来&quot;">​</a></h2><p>手敲长命令迟早会漏参数，而漏掉的往往正是踩过坑才加上的那个。<code>environments/ego-humble/</code> 下的三个脚本把这些坑固化了，<strong>直接在仓库里运行即可</strong>，不需要抄到别处。</p><table tabindex="0"><thead><tr><th>脚本</th><th>干什么</th><th>幂等吗</th></tr></thead><tbody><tr><td><code>fetch-source.sh</code></td><td>克隆 EGO 源码并<strong>钉死在一个 commit</strong></td><td>是，已存在就只校验</td></tr><tr><td><code>docker-run.sh</code></td><td>开一个交互式容器</td><td>是</td></tr><tr><td><code>run-sim.sh</code></td><td>起停整套仿真（<code>start</code> / <code>stop</code> / <code>status</code> / <code>logs</code>）</td><td>是，<code>start</code> 会先清掉同名容器</td></tr></tbody></table><h3 id="_8-1-fetch-source-sh-把上游钉住" tabindex="-1">8.1 <code>fetch-source.sh</code>：把上游钉住 <a class="header-anchor" href="#_8-1-fetch-source-sh-把上游钉住" aria-label="Permalink to &quot;8.1 \`fetch-source.sh\`：把上游钉住&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> environments/ego-humble/fetch-source.sh</span></span></code></pre></div><p>【运行验证】在已克隆的机器上运行，输出：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>工作空间: /home/yusei/Documents/Codex/ego-humble</span></span>
<span class="line"><span>目标 commit: 23a8d5a191711dd65633df689bd00f55d4dea8f9</span></span>
<span class="line"><span>✅ 已在目标 commit，无需操作。</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">为什么非要钉住 commit</p><p>上游 <code>ZJU-FAST-Lab/ego-planner-swarm</code> 随时会更新。如果脚本只写「clone 一下」，别人明天复现出来的就不是同一份代码，<strong>本文档里的所有行号、错误信息、参数都会对不上</strong>。</p><p>写死 commit 是「能跑一次」和「可复现」的分界线。<strong>怎么记</strong>：文档引用了行号，就必须锁定版本，否则文档在上游第一次提交后就开始腐烂。</p></div><h3 id="_8-2-docker-run-sh-交互式容器" tabindex="-1">8.2 <code>docker-run.sh</code>：交互式容器 <a class="header-anchor" href="#_8-2-docker-run-sh-交互式容器" aria-label="Permalink to &quot;8.2 \`docker-run.sh\`：交互式容器&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> environments/ego-humble/docker-run.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # 进 bash</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> environments/ego-humble/docker-run.sh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> topic</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 跑一条就退出</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EGO_WORKSPACE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/别的/路径</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> environments/ego-humble/docker-run.sh</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">这里修过一个真实的 bug</p><p>早先版本用 <code>root_dir=&quot;$(cd &quot;$(dirname &quot;$0&quot;)&quot; &amp;&amp; pwd)&quot;</code> 当挂载源，也就是<strong>脚本自己所在的目录</strong>。从学习仓库里执行时，它会把 <code>environments/ego-humble/</code> 挂成 <code>/workspace</code>，容器里根本看不到 EGO 源码。</p><p>现在改成从 <code>EGO_WORKSPACE</code> 取（默认 <code>~/Documents/Codex/ego-humble</code>），并在启动前检查该目录下有没有 <code>src/</code>，没有就报错退出。</p><p><strong>怎么记</strong>：<code>dirname &quot;$0&quot;</code> 回答的是「脚本在哪」，不是「数据在哪」。这两者不是一回事时，就不能用它。</p></div><p>脚本里 <code>sudo --preserve-env=DISPLAY,...</code> 的作用：<code>sudo</code> 默认清空环境变量（安全考虑），所以要显式声明哪些允许穿透。<code>-e DISPLAY</code>（不带值）表示「从当前环境取同名变量」，两者配合才能把 <code>DISPLAY=:1</code> 送进容器。</p><p>它还会自动扫描 <code>/dev/dri/card*</code> 和 <code>/dev/dri/renderD*</code> 并逐个 <code>--device</code> 传进去 —— 漏掉这一步，Mesa 会回落到 <code>llvmpipe</code> 软件渲染。</p><h3 id="_8-3-run-sim-sh-一条命令起停仿真" tabindex="-1">8.3 <code>run-sim.sh</code>：一条命令起停仿真 <a class="header-anchor" href="#_8-3-run-sim-sh-一条命令起停仿真" aria-label="Permalink to &quot;8.3 \`run-sim.sh\`：一条命令起停仿真&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> environments/ego-humble/run-sim.sh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> environments/ego-humble/run-sim.sh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> environments/ego-humble/run-sim.sh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> environments/ego-humble/run-sim.sh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stop</span></span></code></pre></div><p>【运行验证】<code>start</code> 的真实输出：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>→ 启动 ego_sim（规划 + 假飞机 + 假地图，日志上限 20m）</span></span>
<span class="line"><span>→ 启动 ego_rviz（可视化，带 8 个 GPU 设备参数，日志上限 10m）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>✅ 已启动。验收三步：</span></span>
<span class="line"><span>   1) bash run-sim.sh status          节点应有 7 个</span></span>
<span class="line"><span>   2) 看 RViz 窗口里有没有绿色轨迹在动</span></span>
<span class="line"><span>   3) bash run-sim.sh logs            应看到 FSM 状态在变化</span></span></code></pre></div><p><code>status</code> 的真实输出（节点 7 个 + GPU 设备确实进到了容器里）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ego_rviz	Up 29 seconds</span></span>
<span class="line"><span>ego_sim	Up 32 seconds</span></span>
<span class="line"><span>--- 节点 ---</span></span>
<span class="line"><span>/drone_0_ego_planner_node      /drone_0_odom_visualization</span></span>
<span class="line"><span>/drone_0_pcl_render_node       /drone_0_poscmd_2_odom</span></span>
<span class="line"><span>/drone_0_traj_server           /random_forest              /rviz</span></span>
<span class="line"><span>--- GPU 设备是否进到容器里（空 = RViz 会退化成 llvmpipe 软件渲染）---</span></span>
<span class="line"><span>card1 card2 renderD128 renderD129</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">脚本里固化了哪三个坑</p><table tabindex="0"><thead><tr><th>固化的参数</th><th>不加会怎样</th></tr></thead><tbody><tr><td><code>--log-opt max-size / max-file</code></td><td>容器日志无上限。我们真实见过它涨到 <strong>6,938,649 行</strong></td></tr><tr><td><code>--device /dev/dri/*</code></td><td>RViz 退化成 <code>llvmpipe</code> 软件渲染，卡顿</td></tr><tr><td><code>-e DISPLAY=&quot;$DISPLAY&quot;</code>（显式传值）</td><td>依赖 <code>sudo</code> 是否保留变量，换机器就可能开不出窗口</td></tr></tbody></table><p>这就是「环境配好了」的真正含义：<strong>不是我这台机器能跑，而是踩过的坑不会再被漏掉。</strong></p></div><h2 id="记忆卡" tabindex="-1">记忆卡 <a class="header-anchor" href="#记忆卡" aria-label="Permalink to &quot;记忆卡&quot;">​</a></h2><p><strong>卡 1：为什么用 Docker</strong></p><ul><li>一句话理解：ROS 2 版本和 Ubuntu 版本硬绑定，宿主机是 24.04/Jazzy，而 EGO 要 22.04/Humble，所以租一间 22.04 的&quot;房子&quot;。</li><li>三个关键词：版本绑定、宿主机零污染、删掉即还原。</li><li>输入：一个 <code>Dockerfile</code>。</li><li>处理：<code>docker build</code> 逐层安装 ROS + PCL + Armadillo。</li><li>输出：<code>local/ego-planner-humble:latest</code>（4.84 GB）。</li></ul><p><strong>卡 2：环境是怎么加载的</strong></p><ul><li>一句话理解：ROS 命令要先 <code>source</code> 才存在，这件事被塞进了 ENTRYPOINT。</li><li>三个关键词：source、ENTRYPOINT、exec 不走 ENTRYPOINT。</li><li>输入：<code>docker run &lt;镜像&gt; &lt;命令&gt;</code>。</li><li>处理：<code>entrypoint.sh</code> 先 source ROS，再 source <code>install/</code>（如果有），然后 <code>exec &quot;$@&quot;</code>。</li><li>输出：一个 ROS 环境已就绪的进程，且它是容器的 PID 1。</li></ul><h2 id="自测题" tabindex="-1">自测题 <a class="header-anchor" href="#自测题" aria-label="Permalink to &quot;自测题&quot;">​</a></h2><p><strong>1. 为什么不能直接在 Ubuntu 24.04 上 <code>apt install ros-humble-desktop</code>？</strong></p><details class="details custom-block"><summary>答案</summary><p>因为 ROS 2 发行版和 Ubuntu 版本是硬绑定的：Humble 只为 22.04 (Jammy) 编译，Jazzy 只为 24.04 (Noble) 编译。官方源里根本没有面向 24.04 的 Humble 包。</p></details><p><strong>2. 为什么不用 <code>sudo usermod -aG docker $USER</code>？</strong></p><details class="details custom-block"><summary>答案</summary><p><code>docker</code> 组权限等价于 root，而且是永久、无提示的。本项目改用一条只覆盖 <code>/usr/bin/docker</code> 的 sudoers 免密规则——范围明确、可审计、能一条命令撤销。但要清楚：免密 <code>sudo docker</code> 本身仍然等于 root，只适合个人单机学习环境。</p></details><p><strong>3. <code>sudo docker exec ego_sim ros2 node list</code> 报 <code>command not found</code>，为什么？</strong></p><details class="details custom-block"><summary>答案</summary><p><code>docker exec</code> 不执行镜像的 ENTRYPOINT，而 ROS 环境正是在 <code>entrypoint.sh</code> 里 source 的。要自己带上 <code>bash -lc &#39;source /opt/ros/humble/setup.bash &amp;&amp; source /workspace/install/setup.bash &amp;&amp; ...&#39;</code>。</p></details><p><strong>4. Dockerfile 为什么把 <code>ros-humble-pcl-ros</code> 单独放在第二个 <code>RUN</code> 里？</strong></p><details class="details custom-block"><summary>答案</summary><p>它是编译报错之后才补上的。Docker 每条 <code>RUN</code> 是一层缓存，把后加的东西放最后一层，重新构建时前面那一大层能直接命中缓存，省十几分钟。</p></details><p><strong>5. <code>entrypoint.sh</code> 里为什么要写 <code>exec &quot;$@&quot;</code> 而不是直接 <code>&quot;$@&quot;</code>？</strong></p><details class="details custom-block"><summary>答案</summary><p><code>exec</code> 用目标程序<strong>替换</strong>当前进程，这样它就是容器的 PID 1，<code>docker stop</code> 发的 SIGTERM 能直接送到它手上，容器才能正常退出。不加 <code>exec</code> 的话真正的程序是 shell 的子进程，收不到信号。</p></details><p><strong>下一步</strong> → <a href="/drone-learning-lab/ego-planner/build">第二步：编译工作空间</a></p>`,97)])])}const u=a(n,[["render",d]]);export{k as __pageData,u as default};
